# حل تمرين 06

الناتج هو:

```text
3
1
True
True
```

## السبب

- `10 // 3` يعطي `3` لأن القسمة الصحيحة تتجاهل الجزء العشري.
- `10 % 3` يعطي `1` وهو باقي القسمة.
- `x > 5 and y < 5` كلا الشرطين صحيح، لذلك النتيجة `True`.
- `4 not in items` صحيح لأن 4 غير موجودة في القائمة.

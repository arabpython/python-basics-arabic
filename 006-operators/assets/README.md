# Assets

هذا المجلد اختياري.

يمكن وضع صور الشرح الخاصة بالدرس هنا لاحقًا، مثل:

- صورة جدول العوامل الحسابية.
- صورة الفرق بين `/` و `//`.
- صورة الفرق بين `==` و `is`.
- صورة عوامل العضوية `in` و `not in`.

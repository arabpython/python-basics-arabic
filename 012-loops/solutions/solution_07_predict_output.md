# حل تمرين 07

الناتج هو:

```text
1
3
4
```

## السبب

- عندما `i = 1` تتم الطباعة.
- عندما `i = 2` يتم تنفيذ `continue` لذلك لا يطبع 2.
- عندما `i = 3` تتم الطباعة.
- عندما `i = 4` تتم الطباعة.
- عندما `i = 5` يتم تنفيذ `break` وتتوقف الحلقة قبل الطباعة.

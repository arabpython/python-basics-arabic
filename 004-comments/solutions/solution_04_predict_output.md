# حل تمرين 04

الناتج سيكون:

```text
A
D
```

## السبب

- السطر `print("A")` يعمل بشكل طبيعي.
- السطر `# print("B")` تعليق، لذلك لا يعمل.
- السطر `print("C")` موجود داخل نص متعدد الأسطر بين `"""`، لذلك لن يظهر في الناتج.
- السطر `print("D")` يعمل بشكل طبيعي.

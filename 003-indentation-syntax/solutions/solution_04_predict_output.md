# حل تمرين 04

الناتج هو:

```text
A
B
C
D
```

## السبب

- `x = 10`
- الشرط الأول `x > 5` صحيح، لذلك يطبع `A`.
- الشرط المتداخل `x > 8` صحيح، لذلك يطبع `B`.
- بعد الخروج من الشرط المتداخل، ما زلنا داخل الشرط الأول، لذلك يطبع `C`.
- في النهاية يخرج من كل الكتل ويطبع `D`.

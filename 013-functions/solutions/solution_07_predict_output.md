# حل تمرين 07

الناتج هو:

```text
10
Hello Guest
Hello Ali
```

## السبب

- `add(2, 3)` ترجع `5`.
- `x * 2` يساوي `10`.
- `greet()` تستخدم القيمة الافتراضية `Guest`.
- `greet("Ali")` تستخدم القيمة المرسلة عند الاستدعاء.

# حل تمرين 09

محتوى الملف النهائي سيكون:

```text
C
```

## السبب

- أول `w` كتب `A`.
- بعدها `a` أضاف `B` في النهاية.
- آخر `w` فتح الملف للكتابة من البداية، فمسح المحتوى القديم وكتب `C` فقط.

قاعدة مهمة:

- `w` يكتب من البداية ويمسح القديم.
- `a` يضيف في نهاية الملف بدون حذف القديم.

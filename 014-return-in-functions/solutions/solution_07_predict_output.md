# حل تمرين 07

الناتج هو:

```text
5
None
10
```

## السبب

- `add_print(2, 3)` تطبع `5` لكنها لا ترجع قيمة، لذلك `x = None`.
- `add_return(2, 3)` ترجع `5`، لذلك `y = 5`.
- عند طباعة `x` يظهر `None`.
- عند طباعة `y * 2` يظهر `10`.

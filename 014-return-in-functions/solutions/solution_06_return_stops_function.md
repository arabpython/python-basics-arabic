# حل تمرين 06

الناتج هو:

```text
A
B
```

## السبب

- الدالة تطبع `A`.
- بعدها تنفذ `return "B"` فتتوقف الدالة.
- السطر `print("C")` لن يعمل لأنه بعد `return`.
- خارج الدالة نطبع قيمة `result` وهي `B`.
